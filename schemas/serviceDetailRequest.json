{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "title": "serviceDetailRequest",
  "id": "serviceDetailRequest",
  "description": "A collection of details of a service. These details are used for Serviceability Requests.",
  "type": "object",
  "required": [
      "objectType"
  ],
  "properties": {
    "objectType": {
      "type": "string",
      "enum": [
          "serviceDetailRequest"
      ],
      "description": "The type of the object, serviceDetailRequest."
    },
    "serviceCategory": {
      "type": "string",
      "enum": [
        "ETHERNET"
      ],
      "description": "A high-level categorization of products such as Ethernet.",
      "title": "Service Category"
    },
    "serviceType": {
      "type": "string",
      "enum": [
        "ACCESS_EPL",
        "UNI"
      ],
      "description": "The specific type of product being detailed (e.g. ACCESS_EPL).",
      "title": "Service Type"
    },
    "serviceDetailItems": {
      "type": "array",
      "items": {
        "oneOf": [
          { "$ref": "ethernetDetailItemRequest.json#" }
        ]
      },
      "description": "Additional service capability details.",
      "title": "Service Detail Items"
    }
  }
}
